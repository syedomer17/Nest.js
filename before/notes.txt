✅ 1. Classes and Constructors
📘 What is a Class?
A class is a blueprint for creating objects. It defines properties (data) and methods (functions).

📘 What is a Constructor?
A constructor is a special method in a class that runs automatically when you create a new object from the class. It’s used to initialize values.

✅ 2. Access Modifiers (public, private, readonly)
These define who can access a class’s properties or methods.

Modifier	Meaning
public	(default) Accessible anywhere
private	Accessible only inside the class
readonly	Value cannot be changed after initialization

✅ 3. Generics (<T>)
Generics let you create reusable components or functions that work with any data type.

✅ 4. Decorators (Used in NestJS)
A decorator is a special function used to add metadata or modify classes, methods, or properties. In NestJS, you’ll see them everywhere (e.g., @Controller(), @Get()).

Think of a decorator like a label or wrapper that gives extra behavior to something.